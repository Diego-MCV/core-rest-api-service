# Dependencias (esta carpeta es gigante, no se sube)
node_modules/

# Archivos de compilación (código traducido a JS)
dist/

# Variables de entorno (¡AQUÍ ESTÁN TUS CONTRASEÑAS!)
.env
.env.production
.env.staging

# Logs y archivos temporales
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Archivos de caché de TypeScript
*.tsbuildinfo

# Docker (si usas volúmenes locales para la DB)
pgdata/